---
# defaults file for docker

# Should be overrridden
#docker_user: "{{ ansible_user_id }}"
docker_user: "dockeruser"
docker_datadir : "docker-data"

docker:
  packages:
    - "docker-ce"
    - "docker-ce-cli"
    - "containerd.io"
    - "python3-docker"
    - "docker-compose-plugin"

  service:
    name: "docker"
    #TODO: Currently not implemented, pending testing in containers
    #subnet: "10.10.11.0/24" # Chose something that does not conflict with your existing networks

      #\\ {
      #\\   "default-address-pools":
      #\\   [
      #\\     { "base":"{TODO{ docker.service.subnet }TODO}","size":{TODO{ docker.service.subnet.split('/')[1] }TODO} }
      #\\   ]
      #\\ }

